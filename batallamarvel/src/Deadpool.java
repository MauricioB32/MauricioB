public class Deadpool extends Personaje {
    
    public Deadpool(int vidaInicial) {
        // Llama al constructor de la clase base con los valores de Deadpool
        super("Deadpool", vidaInicial, 10, 100, 0.25);
    }
}